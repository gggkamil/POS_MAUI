
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="CashierApp.ProductSummaryPage"
             Title="Product Summary">

    <ScrollView>
        <Grid x:Name="ProductSummaryGrid" Padding="10" VerticalOptions="FillAndExpand">
            <!-- Headers -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Text="Product" FontAttributes="Bold" HorizontalOptions="Center"/>
            <Label Grid.Row="0" Grid.Column="1" Text="Quantity" FontAttributes="Bold" HorizontalOptions="Center"/>
        </Grid>
    </ScrollView>
</ContentPage>